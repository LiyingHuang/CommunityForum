<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coral.community.dao.UserMapper">

    <sql id="selectFields">
        id, username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>

    <sql id="insertFields">
        username,password,salt,email,type,status,activation_code,header_url,create_time
    </sql>

<!--get-->
    <!--tag corresponding GET/UPDATE/DELETE/method-->
    <!--com.coral.community.entity.User-->
    <!--select id="METHOD NAME" resultType="ABSOLUTE PATH" (should be "com.coral.community.entity.User", but we already set in the application properties that com.coral.community.entity, so we can just use "User")-->
    <!--#{UserField}-->
    <select id="selectById" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where id = #{id}
    </select>

    <select id="selectByName" resultType="User" >
        select <include refid="selectFields"></include>
        from user
        where username = #{username}
    </select>

    <select id="selectByEmail" resultType="User" >
        select <include refid="selectFields"></include>
        from user
        where email = #{email}
    </select>

<!--insert/update-->
    <!--#{UserField}-->
    <!--insert id="MethodName" parameterType="pass-into ParameterType" keyProperty="auto-increasing/primaryKey"-->
    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user(<include refid="insertFields"></include>)
        values(#{username},#{password},#{salt},#{email},#{type},#{status},#{activationCode},#{headerUrl},#{createTime})
    </insert>

    <update id="updateStatus">
        update user set status = #{status} where id = #{id}
    </update>

    <update id="updateHeader">
        update user set header_url = #{headerUrl} where id = #{id}
    </update>

    <update id="updatePassword">
        update user set password =#{password} where id = #{id}
    </update>

</mapper>
<!--<mapper namespace="com.coral.community.dao.UserMapper">-->
<!--    <sql id="insertFields">-->
<!--        username, password, salt, email, type, status, activation_code, header_url, create_time-->
<!--    </sql>-->
<!--    <sql id="selectFields">-->
<!--        id, username, password, salt, email, type, status, activation_code, header_url, create_time-->
<!--    </sql>-->
<!--    <select id="selectById" resultType="User">-->
<!--        select <include refid="selectFields"></include>-->
<!--        from user-->
<!--        where id = #{id}-->
<!--    </select>-->
<!--    <select id="selectByName" resultType="User">-->
<!--        select <include refid="selectFields"></include>-->
<!--        from user-->
<!--        where username = #{username}-->
<!--    </select>-->
<!--    <select id="selectByEmail" resultType="User">-->
<!--        select <include refid="selectFields"></include>-->
<!--        from user-->
<!--        where email = #{email}-->
<!--    </select>-->
<!--    <insert id="insertUser" parameterType="User" keyProperty="id">-->
<!--        insert into user (<include refid="insertFields"></include>)-->
<!--        values(#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl}, #{createTime})-->
<!--    </insert>-->
<!--    <update id="updateStatus">-->
<!--        update user set status = #{status} where id = #{id}-->
<!--    </update>-->
<!--    <update id="updateHeader">-->
<!--        update user set header_url = #{headerUrl} where id = #{id}-->
<!--    </update>-->
<!--    <update id="updatePassword">-->
<!--        update user set password = #{password} where id = #{id}-->
<!--    </update>-->
<!--</mapper>-->
